#!/usr/bin/env bash
set -euo pipefail
root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
mkdir -p "$HOME/.zinc"
cp "$root/config/zinc.models.yaml" "$HOME/.zinc/config.yaml"
chmod +x "$root/adapters/responses.py"
echo "installed openai-responses Zinc config to $HOME/.zinc/config.yaml"
